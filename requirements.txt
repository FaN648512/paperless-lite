# paperless-lite 运行依赖
# ==========================
# 安装：pip install -r requirements.txt
#
# ⚠️ 关键约束：onnxruntime 版本
#   rapidocr-onnxruntime 依赖 onnxruntime 做 CPU 推理。
#   onnxruntime >= 1.21 的 CPU 版需要 AVX2 指令集，
#   在 2011 年前后的老 CPU（如 Intel Pentium G 系列）上会直接崩溃。
#   本项目实测锁 1.20.1 可稳定运行。

# ---------- Web 框架 ----------
Flask==3.1.3
Werkzeug==3.1.8

# ---------- OCR：RapidOCR（PP-OCRv4）+ onnxruntime ----------
rapidocr-onnxruntime==1.2.3
onnxruntime==1.20.1
opencv-python==5.0.0.93
numpy==2.5.2

# ---------- PDF / Office 解析 ----------
pymupdf==1.28.2
openpyxl==3.1.5
xlrd==2.0.2
python-pptx==1.0.2

# ---------- 图片处理（缩略图 / 长图切分）----------
pillow==12.3.0
